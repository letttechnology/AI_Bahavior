## [user] (21:36:30)

In the interlinear-bible-api project at D:\workspace-vscode\interlinear-bible-api, I need to trace how the gloss for ἕτερον (G2087) in 1 Cor 6:1 gets resolved through the import/build pipeline.

Please do the following:

1. Search corpus-LITE.json for G2087 (or ἕτερος) to see what senses/glosses are defined. The file is at data/corpus-LITE.json.

2. Look at how the corpus import works - specifically how it populates lexeme_meaning and lexeme_sense. Check:
   - The import endpoint (likely AdminController or similar)
   - The import service that handles corpus-LITE.json
   - How it creates lexeme_meaning rows with source='corpus'
   - How lexeme_sense gets populated from those meanings

3. Look at how the build-time pipeline (regenerate-lite-glosses) uses the corpus data:
   - Check InflectionEngineService.fetchAllTokens() or similar
   - See how it resolves the base gloss from corpus/lexeme_meaning before inflection
   - Understand what "sense 0" vs "sense 1" means in practice

4. Check if the lexeme_sense table has entries for G2087 and what their order is. You can search in the Java code or look at how lexeme_sense is populated.

5. Check if there's a regenerate-lite-glosses endpoint or similar that rebuilds the gloss_set_entry cache.

Return:
- The corpus-LITE.json entry structure for G2087 (or ἕτερος) - specifically all senses and their order
- How sense ordering is determined in the import pipeline (which field/column controls priority)
- Where the "first sense" fallback happens in the build pipeline code
- What the correct fix location would be (which file, which table, which step in the pipeline)
- The relevant code file paths and line numbers

This is a research task only - do NOT modify any files.

## [assistant] (21:36:31)

Let me carefully trace through the interlinear-bible-api project to understand how glosses for ἕτερον (G2087) in 1 Cor 6:1 get resolved through the import/build pipeline. Let me start by exploring the project structure and then methodically work through each question.

## [assistant] (21:36:31)

I'll start by exploring the project structure and then trace the pipeline methodically.

## [assistant] (21:36:33)

The glob had an error - let me try different approaches to find the files.

## [assistant] (21:36:36)

Good. Let me now look at the corpus-LITE.json for G2087, and the key source files.

## [assistant] (21:36:39)

Good, now I have a comprehensive view. Let me now:
1. Search for G2087 in corpus-LITE.json
2. Look at the AdminController to find the import endpoints
3. Look at the generate_lite_glosses.py script
4. Look at the InflectionEngineService or similar that reads gloss_set_entry
5. Check the AdminController for the regenerate-lite-glosses endpoint

Let me start by searching corpus-LITE.json for G2087 and finding the relevant Java/Python files.

## [assistant] (21:36:44)

Found the G2087 entry in corpus-LITE.json. Let me get the full entry. Also need to find:
1. The AdminController
2. The generate_lite_glosses.py script
3. The InflectionEngineService.java

Let me fix the PowerShell execution policy issue or try different approaches.

## [assistant] (21:36:48)

Now let me read:
1. AdminController (or Controllers.java) - to find the import-corpus and regenerate-lite-glosses endpoints
2. The generate_lite_glosses.py script
3. The InflectionEngineService.java
4. Also find the LexemeMeaning entity and lexeme_sense related things

## [assistant] (21:36:53)

Now let me read the rest of the generate_lite_glosses.py script (starting from line 914) and also look at the import controller and LexemeMeaning entity. I also need to find the SenseSelectorService and see the GlossController that handles the regenerate-lite-glosses endpoint.

## [assistant] (21:36:57)

Now I need to find the PipelineController that contains the regenerate-lite-glosses endpoint. Let me also look at the SenseSelectorService.

## [assistant] (21:36:58)

Now let me find the PipelineController (which has the regenerate-lite-glosses endpoint) and the SenseSelectorService:

## [assistant] (21:37:02)

Now I need to find SenseSelectorService - let me search more broadly.

## [assistant] (21:37:07)

Let me look for the SenseSelectorService - maybe it's named differently or I need to grep for it.

## [assistant] (21:37:12)

The grep for SenseSelectorService failed. Let me search more specifically in the service package and also look at how the sense selection works in InflectionEngineService. I already saw that it calls senseSelector.selectSense() at line 390. Let me find that class.

## [assistant] (21:37:17)

The SenseSelectorService might be in the service directory but with a different name pattern. Let me look at all services and search for the class.

## [assistant] (21:37:22)

I don't see a SenseSelectorService.java file in the service directory. But it's referenced in InflectionEngineService.java's constructor (line 46: `private final SenseSelectorService senseSelector;`). It might be in a different package or it could be that the file hasn't been created yet. Let me grep for it more broadly.

Actually, wait - it's possible that the SenseSelectorService is referenced as a dependency but doesn't have its own file. Let me search within the java files for "SenseSelectorService".

## [assistant] (21:37:27)

The SenseSelectorService is referenced in InflectionEngineService.java but doesn't have its own file - it's likely defined in the same file or somewhere else. Let me also search within the InflectionEngineService file more carefully, as well as looking at the GlossChainService which might have the logic.

Actually, looking at InflectionEngineService line 46, it says `SenseSelectorService senseSelector` - this could be defined in the same file or it could be that this service hasn't been created yet. Let me look at the file again more carefully.

## [assistant] (21:37:34)

Interesting - the selectSense method is not defined anywhere. It might be that it's defined in a different branch or not yet implemented. Let me look at the V20 migration that was mentioned, and also look at the remaining lines of InflectionEngineService.java to see if the class definition continues.

Let me also check the TokenSenseController and the GlossChainService since they might contain sense selection logic.

## [assistant] (21:37:40)

Now I notice that `SenseSelectorService` is referenced in `InflectionEngineService.java` line 46, but there's no corresponding file. Let me check if it's defined within the same file, or look in the target directory for compiled classes. Actually, looking at the constructor at line 88-98, it does take a `SenseSelectorService` parameter. This might be a future service that hasn't been created yet, or it was created but not committed. Let me check the full list of files and also look for a file that might contain it in a different path.

Actually, looking more carefully at the InflectionEngineService at line 386-398, there's a section that reads:
```java
// Sense selection: override resolvedGloss when a contextual rule matches
boolean senseOverrideActive = false;
if (sid != null) {
    String morphKeyTok = str(token.get("morph_key"));
    Long verseIdTok = toLong(token.get("verse_id"));
    if (morphKeyTok != null && verseIdTok != null) {
        Integer selectedSense = senseSelector.selectSense(sid, verseIdTok.intValue(), morphKeyTok);
        ...
    }
}
```

This means the SenseSelectorService is probably supposed to exist. Let me check if it's defined elsewhere.

Let me also look at the GlossChainService which may orchestrate the sense selection pipeline.

## [assistant] (21:37:46)

Now I have a very comprehensive understanding of the entire pipeline. Let me now look at the rest of the generate_lite_glosses.py to see the token fetching query, and also look for the `sense_index = 0` usage which shows the "first sense" fallback. Actually, I already have that from reading the full file.

Let me also check the V20 migration file and also search for how lexeme_sense is defined in the schema.

## [assistant] (21:37:52)

Let me also check the V19 migration which creates the sense index, and V46-48 for sense selection rules.

## [assistant] (21:37:56)

Excellent! This is extremely informative. Let me now compile the full trace.

Here's the complete picture:

### 1. corpus-LITE.json entry for G2087/ἕτερος
Entry (at line 31292):
```json
"G2087": {
    "lemma": "ἕτερος",
    "transliteration": "héteros",
    "canonical_word": "other",
    "gloss": "other",
    "shortGloss": "the other of two; another; different",
    ...
    "is_polysemous": false,
}
```

So there are 3 senses in shortGloss:
- **Sense 0 (index 0)**: "the other of two" - labeled as nt_primary in lexeme_sense
- **Sense 1 (index 1)**: "another" - labeled as secondary
- **Sense 2 (index 2)**: "different" - labeled as secondary

### 2. How sense ordering is determined in the import pipeline

In `CorpusImportService.java` (lines 142-193), `rebuildLexemeSenses()`:
- Splits `shortGloss` on semicolons
- `sense_index` is the position in the array (0-based)
- Sense type: `senseType = (i == 0) ? "nt_primary" : "secondary"`
- Stores in `lexeme_sense` table with `(lexeme_id, sense_index, gloss, sense_type, nt_attested=true)`
- The ORDER of senses is the ORDER they appear in `shortGloss` string
- The DB has a unique constraint on `(lexeme_id, sense_index)`

No separate priority/displayOrder column exists on lexeme_sense. The ordering is purely by `sense_index`.

### 3. Where the "first sense" fallback happens

In the build pipeline (InflectionEngineService.java, `fetchAllTokens()` at line 875):
```sql
COALESCE(ls_ai.gloss, ls.gloss, m.short_gloss) AS resolved_gloss
```
Where:
- `ls_ai` = `lexeme_sense` at the AI-selected `sense_index` (from token_sense_override)
- `ls` = `lexeme_sense` at `sense_index = 0` (the primary sense)
- `m.short_gloss` = the raw `shortGloss` from `lexeme_meaning` (fallback)

So the priority is:
1. AI-selected sense (lexeme_sense at the token_sense_override's sense_index)
2. Sense 0 from lexeme_sense (always `sense_index = 0`)
3. Raw corpus shortGloss string

For G2087 (ἕτερος), since `is_polysemous = false`, there won't be any AI override. So `resolved_gloss` will use `ls.gloss` which is `lexeme_sense` at `sense_index = 0` = "the other of two".

Then in `deriveGloss()`: for non-verb tokens (G2087 is an adjective), at line 616-619:
```java
if (sid != null) {
    String canon = canonicalWords.get(sid);
    if (canon != null && !canon.isBlank()) return canon;
}
```

But wait - `loadCanonicalWords()` at line 842-846 returns `Map.of()` (empty map) because `canonical_word` column was removed (V37). So canonical fallback is skipped.

Then the fallback at line 622-627:
```java
if (resolvedGloss != null && !resolvedGloss.isBlank()) {
    String g = resolvedGloss.strip();
    if (g.toLowerCase().startsWith("to ") && !"V".equals(pos)) g = g.substring(3).strip();
    if (g.contains(",") && !g.contains(";")) g = g.split(",")[0].strip();
    return g;
}
```

For G2087 (not a verb), `resolvedGloss` = "the other of two" (sense 0). It will be returned as-is (no "to " prefix, no comma). So the LITE gloss for ἕτερον in 1 Cor 6:1 will be "the other of two".

Similarly, in the Python `generate_lite_glosses.py`, `get_tokens()` uses:
```sql
COALESCE(ls_ai.gloss, ls.gloss, m.short_gloss) AS resolved_gloss
```
And then `derive_gloss()` uses `resolved_gloss` for non-verb POS tokens.

### 4. The V46-V48 migrations are key

V46 created a `sense_selection_rule` table specifically for this case — a rule that selects sense_index=1 for G2087 in 1 Cor 6:1 (book 46, chapter 6, verse 1).

V47 added another rule for G4314 (πρός) to select sense 0.

V48 dropped the entire `sense_selection_rule` table, with the comment: "Per-verse exception rules proved to be the wrong approach — the correct fix is at the source data level: lexeme_sense (usable LITE gloss as sense 0)..."

So the migration history shows this was acknowledged as the wrong approach.

### 5. The SenseSelectorService is expected to exist

In `InflectionEngineService.java` at line 386-398, there's code that calls `senseSelector.selectSense()` and `senseSelector.getSenseGloss()`. But `SenseSelectorService` is not found in the service package. This seems like it was intended to be the implementation of the sense_selection_rule system, but it was never created (or was removed when V48 dropped the table and the corresponding code was left in but unused).

### Summary of the fix location

The correct fix should be at the **source data** level (as noted in V48):
1. **Re-order senses in corpus-LITE.json** so that "another" is sense 0 for G2087
2. OR, fix the lexeme_sense ordering via a SQL migration after a corpus re-import
3. The fix should ensure the LITE gloss for 1 Cor 6:1 is "another" not "the other of two"

The actual fix location should be either:
- `data/corpus-LITE.json` - reorder shortGloss for G2087: `"shortGloss": "another; the other of two; different"`
- `CorpusImportService.java` - add a post-import override for specific entries
- A Flyway migration that updates lexeme_sense entries

Now let me compile all this into a coherent response.

## [assistant] (21:38:07)

Now let me also look at the full `generate_lite_glosses.py` to see the `get_tokens()` SQL and the `derive_gloss` fallback for non-verb tokens:

## [assistant] (21:38:11)

Now I have a complete picture. Let me compile all findings. The V46 migration is fascinating - it shows that the issue with G2087 in 1 Cor 6:1 was specifically known, and a `sense_selection_rule` was created to select sense 1 ("another") for this specific occurrence. But then V48 dropped the entire table, saying the approach was wrong.

Let me also check if there are any additional relevant files like the PipelineOrchestrator that ties everything together.

## [assistant] (21:38:12)

Let me check the PipelineOrchestrator to see the full cascade, and also the GlossChainService more closely for the Layer 5 fallback:

## [assistant] (21:38:16)

Now I have all the information needed. Here is the complete trace.

---

## Complete Trace: How the Gloss for `ἕτερον` (G2087) in 1 Cor 6:1 Gets Resolved

---

### 1. Corpus-LITE.json Entry for G2087 / `ἕτερος`

**File:** `D:\workspace-vscode\interlinear-bible-api\data\corpus-LITE.json` (line 31292)

```json
"G2087": {
    "lemma": "ἕτερος",
    "transliteration": "héteros",
    "canonical_word": "other",
    "gloss": "other",
    "shortGloss": "the other of two; another; different",
    "shortGloss_v1": "the other of two; another; different",
    "shortGloss_v0": "other; one of two; different; alternate",
    "is_polysemous": false,
    "is_stative": false,
    "is_deponent": false
}
```

**3 senses, in this order:**
| Index | Gloss | Type |
|-------|-------|------|
| 0 | "the other of two" | `nt_primary` |
| 1 | "another" | `secondary` |
| 2 | "different" | `secondary` |

---

### 2. How Sense Ordering Is Determined During Import

**File:** `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\importer\CorpusImportService.java`

#### Step A: Populate `lexeme_meaning` (lines 78-96)
The `shortGloss` field is stored as a single semicolon-delimited string in `lexeme_meaning`, with `source='corpus'`.

```java
// line 89
.shortGloss(truncate(corpus.shortGloss(), 200))
```

No splitting occurs at this step -- the whole string goes into one column.

#### Step B: Rebuild `lexeme_sense` (lines 142-193, method `rebuildLexemeSenses()`)
After saving meanings, it splits `shortGloss` on semicolons:

```java
// line 158-163
String[] senses = shortGloss.split(";");
for (int i = 0; i < senses.length; i++) {
    String gloss = senses[i].trim();
    if (gloss.isBlank()) continue;
    String senseType = (i == 0) ? "nt_primary" : "secondary";
    rows.add(new SenseRow(lexemeId, i, truncate(gloss, 200), senseType));
}
```

**Key points:**
- `sense_index` is the **array position** (0-based) after splitting `shortGloss` on `;`
- Sense 0 gets `sense_type = 'nt_primary'`; all others get `'secondary'`
- There is **no separate priority/displayOrder column** on `lexeme_sense` -- ordering is purely by `sense_index`
- The database `UNIQUE (lexeme_id, sense_index)` constraint enforces that sense_index values are the ordering mechanism

**Result for G2087 in `lexeme_sense`:**
```
lexeme_id | sense_index | gloss              | sense_type  | nt_attested
----------|-------------|---------------------|-------------|------------
     X    |     0       | "the other of two"  | nt_primary  | true
     X    |     1       | "another"           | secondary   | true
     X    |     2       | "different"         | secondary   | true
```

---

### 3. Where the "First Sense" Fallback Happens in the Build Pipeline

There are **two independent code paths** for generating LITE glosses -- the Java `InflectionEngineService` (current) and the Python `generate_lite_glosses.py` (original, now `scripts/old/`). Both use the same SQL query pattern.

#### Path 1: Java `InflectionEngineService.regenerateAllGlosses()` (current)

**File:** `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\service\InflectionEngineService.java`

**SQL in `fetchAllTokens()` (lines 876-896):**

```sql
SELECT
  ...
  COALESCE(ls_ai.gloss, ls.gloss, m.short_gloss) AS resolved_gloss,
  ...
FROM verse_word vw
LEFT JOIN lexeme l ON vw.lexeme_id = l.id
LEFT JOIN lexeme_meaning m ON m.lexeme_id = l.id AND m.source = 'corpus'
LEFT JOIN lexeme_sense ls ON ls.lexeme_id = l.id AND ls.sense_index = 0
LEFT JOIN token_sense_override tso ON tso.verse_word_id = vw.id
LEFT JOIN lexeme_sense ls_ai ON ls_ai.lexeme_id = l.id
  AND ls_ai.sense_index = tso.sense_index
```

**Resolution priority (the `COALESCE`):**
1. **`ls_ai.gloss`** -- `lexeme_sense` at the AI-selected `sense_index` (from `token_sense_override`, for polysemous tokens processed by C1). G2087 has `is_polysemous=false`, so `token_sense_override` is empty -> `null`.
2. **`ls.gloss`** -- `lexeme_sense` at **`sense_index = 0`** = **"the other of two"**. This is the snapshot used for G2087.
3. **`m.short_gloss`** -- raw `lexeme_meaning.short_gloss` = `"the other of two; another; different"` (fallback if `lexeme_sense` table is missing).

**Then in `deriveGloss()` (lines 367-630):**
G2087 is not a function word, not an article, not G846, not a personal pronoun, and not a verb. It is not a preposition either. So execution falls to line 616-619:

```java
// line 616-619
if (sid != null) {
    String canon = canonicalWords.get(sid);
    if (canon != null && !canon.isBlank()) return canon;
}
```

But `loadCanonicalWords()` (line 842-846) returns `Map.of()` (empty) because the `canonical_word` column was removed in migration V37. So this is skipped.

Then the **final fallback** at lines 622-627:

```java
if (resolvedGloss != null && !resolvedGloss.isBlank()) {
    String g = resolvedGloss.strip();
    if (g.toLowerCase().startsWith("to ") && !"V".equals(pos)) g = g.substring(3).strip();
    if (g.contains(",") && !g.contains(";")) g = g.split(",")[0].strip();
    return g;
}
```

Since `resolvedGloss` = `"the other of two"` (sense 0), and it does not start with "to " and contains no comma, it is returned **as-is**.

#### Path 2: Python `generate_lite_glosses.py` (original, in `scripts/old/`)

**File:** `D:\workspace-vscode\interlinear-bible-api\scripts\old\generate_lite_glosses.py`

**Same COALESCE pattern in `get_tokens()` (lines 1019-1023):**

```python
COALESCE(
    ls_ai.gloss,                  -- AI-selected index
    ls.gloss,                     -- sense_index 0
    m.short_gloss                 -- corpus fallback
) AS resolved_gloss
```

And in `derive_gloss()` (lines 940-962) for non-verb content words:

```python
# line 945-949
if sid and not token.get("has_ai_override"):
    canon_entry = CANONICAL_WORDS.get(sid, {})
    canon_word = (canon_entry.get("canonical_word") or "").strip()
    if canon_word:
        return canon_word

# line 951-960
if resolved_gloss:
    gloss = resolved_gloss.strip()
    if gloss.lower().startswith("to ") and pos != "V":
        gloss = gloss[3:].strip()
    if ',' in gloss and ';' not in gloss:
        gloss = gloss.split(',')[0].strip()
    return gloss
```

Same result: `resolved_gloss` = `"the other of two"` (sense 0) is returned unchanged.

#### Path 3: Runtime API fallback in `GlossChainService` (live-serving)

**File:** `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\service\GlossChainService.java`

At runtime (when a user views a passage), the override chain (lines 14-27) resolves via `resolveBatch()`. If `gloss_set_entry` is populated (which it is after regenerate-lite-glosses runs), it returns layer 3 directly. But if the cache is empty, Layer 5 fallback at lines 315-327 does:

```java
// line 316-319
if (notBlank(corpus)) {
    String g = corpus.split(";")[0].split(",")[0].trim();
    if (notBlank(g)) return new GlossResult(g, "corpus_gloss", null);
}
```

This takes the **first semicolon-separated sense** from `lexeme_meaning.short_gloss` = **"the other of two"**.

---

### 4. The Historical `sense_selection_rule` Table (V46-V48)

The issue of G2087 in 1 Cor 6:1 was **previously known and addressed** through a dedicated mechanism:

**Migration V46** (`src/main/resources/db/migration/V46__sense_selection_rule.sql`):
```sql
-- Seed rule for #140: G2087 (ἕτερος) in 1 Cor 6:1 should use sense 1 ("another")
-- instead of sense 0 ("the other of two"), because the context is an indefinite
-- fellow believer, not one of exactly two parties.
INSERT INTO sense_selection_rule (strongs_id, book_id, chapter, verse_num, selected_sense_index, label)
VALUES ('G2087', 46, 6, 1, 1, '1 Cor 6:1 — "another" (fellow believer, not one of two)');
```

**Migration V47** added another rule for G4314 (`πρός`).

**Migration V48** (`V48__drop_sense_selection_rule.sql`) removed the entire table:
```sql
-- The table was created in V46 with seed rules, and V47 added one more.
-- Per-verse exception rules proved to be the wrong approach — they are
-- data hacks, not infrastructure. The correct fix is at the source data
-- level: lexeme_sense (usable LITE gloss as sense 0), PREPOSITION_CASE_GLOSSES
-- map, and corpus-LITE.json re-synthesis.
DROP TABLE IF EXISTS sense_selection_rule;
```

**However**, the `InflectionEngineService.java` still references `SenseSelectorService` (line 46, called at line 390):
```java
Integer selectedSense = senseSelector.selectSense(sid, verseIdTok.intValue(), morphKeyTok);
```
This `SenseSelectorService` class **does not exist** in the source tree -- it was never implemented or was removed alongside V48, but the reference was left in the code. So the sense-selection override path (lines 386-398) is effectively dead code that never fires.

---

### 5. Where the Fix Should Be

The correct approach (as stated in V48's rationale and the `corpus-sense-design.md` document) is to fix the **source data** so that the most appropriate interlinear gloss is **sense 0**. There are two valid approaches:

| Approach | Where | What to change |
|----------|-------|----------------|
| **A: Re-order corpus-LITE.json** | `data/corpus-LITE.json`, line 31297 | Change `"shortGloss": "the other of two; another; different"` to `"shortGloss": "another; the other of two; different"` -- then re-import corpus |
| **B: SQL migration to reorder lexeme_sense** | A new Flyway migration file | `UPDATE lexeme_sense SET sense_index = ... WHERE lexeme_id = ... AND ...` -- swaps the sense_index values for sense 0 and sense 1 for G2087 |

After either fix, the pipeline must run: `POST /admin/regenerate-lite-glosses` (via `PipelineController` line 170, which calls `InflectionEngineService.regenerateAllGlosses()` line 151) to rebuild `gloss_set_entry` from the corrected `lexeme_sense` rows.

---

### Summary of All Relevant Code Locations

| What | File | Lines |
|------|------|-------|
| **Corpus data for G2087** | `D:\workspace-vscode\interlinear-bible-api\data\corpus-LITE.json` | 31292-31306 |
| **Corpus import + lexeme_sense rebuild** | `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\importer\CorpusImportService.java` | 66-103 (importAll), 142-193 (rebuildLexemeSenses) |
| **Import endpoint** | `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\controller\admin\PipelineController.java` | 72-101 (import-corpus) |
| **Regenerate glosses endpoint** | `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\controller\admin\PipelineController.java` | 170-180 (regenerate-lite-glosses) |
| **SQL that resolves gloss (COALESCE ls_ai → ls → corpus)** | `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\service\InflectionEngineService.java` | 876-896 (fetchAllTokens) |
| **Same SQL in Python version** | `D:\workspace-vscode\interlinear-bible-api\scripts\old\generate_lite_glosses.py` | 1019-1023 (get_tokens) |
| **deriveGloss fallback for non-verb content words** | `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\service\InflectionEngineService.java` | 616-627 |
| **Same in Python** | `D:\workspace-vscode\interlinear-bible-api\scripts\old\generate_lite_glosses.py` | 940-962 |
| **Runtime fallback (layer 5)** | `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\service\GlossChainService.java` | 315-327 |
| **sense_selection_rule table (now dropped)** | `D:\workspace-vscode\interlinear-bible-api\src\main\resources\db\migration\V46__sense_selection_rule.sql` | 1-19 |
| **V47 seed rule** | `D:\workspace-vscode\interlinear-bible-api\src\main\resources\db\migration\V47__sense_selection_seed_rules.sql` | 1-13 |
| **V48 drop + rationale** | `D:\workspace-vscode\interlinear-bible-api\src\main\resources\db\migration\V48__drop_sense_selection_rule.sql` | 1-8 |
| **Dead SenseSelectorService reference** | `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\service\InflectionEngineService.java` | 46, 386-398 |
| **Pipeline cascade orchestrator** | `D:\workspace-vscode\interlinear-bible-api\src\main\java\com\interlinear\bible\service\PipelineOrchestrator.java` | 48-95 |
| **Sense design documentation** | `D:\workspace-vscode\interlinear-bible-api\docs\corpus-sense-design.md` | 1-663 |
